CMAKE_MINIMUM_REQUIRED(VERSION 2.8.3)
PROJECT(nao_utec)

FIND_PACKAGE(PkgConfig)

FIND_PACKAGE(catkin REQUIRED)

# Since the package has a setup.py
catkin_python_setup()

# catkin_package(
#   INCLUDE_DIRS include
# )

###########
## Build ##
###########

include_directories(
)
